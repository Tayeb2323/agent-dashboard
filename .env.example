# Voice server configuration — copy to .env and fill in.
# NEVER commit the real .env file.

# Gateway bearer token (required) — the voice server exits if this is unset.
GATEWAY_TOKEN=

# Optional overrides (defaults shown)
# PORT=3002
# GATEWAY_URL=http://127.0.0.1:18789
# GATEWAY_MODEL=openclaw
